load W
b = zeros(5,2);
b(1,1) = max(max(W(:,:,replace({'TETCB'}))));
b(1,2) = min(min(W(:,:,replace({'TETCB'}))));
b(2,1) = max(max(W(:,:,raplace({'FFTCB'}))-W(:,:,raplace({'NGTCB'}))));
b(2,2) = min(min(W(:,:,raplace({'FFTCB'}))-W(:,:,raplace({'NGT'}))))
